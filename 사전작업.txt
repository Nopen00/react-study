npm create vite@latest

// 프로젝트명 넣어주고 시작 가능
// npm create vite@latest 프로젝트명
npm create vite@latest react07-hook-enhanced
npm install tailwindcss @tailwindcss/vite
npm install react-icons
// 라우터 쓴다면 이것도
npm install react-router-dom
// 리덕스 쓴다면 이것
npm install react-redux @reduxjs/toolkit
// axios 서버와 소통
npm install axios

vite.config.ts 에 아래 추가
	import tailwindcss from '@tailwindcss/vite'
	디파인 컴피그 안에 tailwindcss(), 

src/main.tsx 에 BrowserRouter 로 App 감싸기

src/index.css 내용 다 지우고 @import "tailwindcss"; 붙여넣기
src/App.css 다지우기
src/App.tsx return문 내부 다 지우기
/* 예시
	import './App.css'

	function App() {
	return (
		<>
		
		</>
	)
	}

	export default App
*/

=====================================

vite : 빌드도구
	개볼중 작성한 여러개의 파일을 하나 또는 몇 개로 묶는 도구

ESList : 문법검사

=====================================

> create-vite hello-react-project

│
◇  Select a framework:
│  React
│
◇  Select a variant:
│  TypeScript + React Compiler
│
◇  Which linter to use?
│  ESLint
│
◇  Install with npm and start now?
│  Yes

=====================================

npm run dev: 작성하는 코드가 실시간으로 반영된(개발 시 사용)

개발 종료 후 bulid

npm run build : 서버에 올려서 사용자들에게 웹페이지로 배포될 파일들로 코드를 모아 컴파일
	> dist 폴더 생성

npm run preview : build를 통해 나온 결과물을 테스트 하는 단계


Card.module.css

기본 : 부모가 렌더링 되면 자식도 렌더링됨
	자식 컴포넌트의 state값이 변경이 되면 자식 컴포넌트만 리렌더링

부모가 렌더링 될때 자식의 컴포넌트에 영향을 끼치지 않는다면 자식의 렌더링 막는게 효율적 => React.memo()

